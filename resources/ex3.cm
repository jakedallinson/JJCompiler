int foo (void) {
    return x();
    return (x + 3) + 4;
}

int bar (void) {
    return 3 + x[3 + y] * y[x[2]];
    return 3 + x[y] * z;
    if (x + x[3] != y + 4) {
        return;
    }
}

int bigBoi (int a, int b, int c) {
    while (c < 100) {
        if (a == b) {
            c = 100;
        } else if (a > b) {
            b = b + 1;
        } else {
            a = a + 1;
            while (b < a) {
                b = b + 1;
            }
        }
        c = c + 1;
    }
}